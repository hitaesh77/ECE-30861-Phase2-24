'''

Runs the Phase 1 CLI against a tiny input file and compares the output lines to a golden expected.ndjson.
No live network: the CLI should support an OFFLINE_MODE or use fixtures so the test is stable.

'''