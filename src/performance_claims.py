def compute(model_url, code_url, dataset_url) -> tuple[float, float]:
    """
    Compute performance claims metric.
    
    Args:
        model_url (str): URL to the model (Hugging Face)
    """
    
    return 0.0, 0.0  # placeholder